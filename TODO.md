# TODO: Update Frontend to Show Collections of Users

## Tasks
- [x] Update views/buddyMatch/index.ejs to display multiple candidate buddies in a grid layout instead of a single match
- [x] Modify the JavaScript in the view to iterate over the candidates array and populate multiple buddy cards
- [x] Add functionality to allow users to select or send requests to individual candidates
- [x] Test the updated frontend to ensure it displays collections correctly

## Notes
- The controller already returns an array of candidates
- Current view assumes a single match; need to change to handle multiple
- Consider adding pagination or filtering if collections are large
